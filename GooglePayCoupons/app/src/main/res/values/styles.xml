<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>

    <!-- Enable window content transitions for shared element transitions -->
    <item name="android:windowContentTransitions">true</item>
    <item name="android:windowActivityTransitions">true</item>

    <!-- Enter and exit transitions for Shared elements -->
    <item name="android:windowSharedElementEnterTransition">@transition/coupon_transform</item>
    <item name="android:windowSharedElementExitTransition">@transition/coupon_transform</item>
  </style>

  <style name="TranslucentActivity" parent="AppTheme">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@color/couponTranslucentOverlay</item>
    <item name="android:statusBarColor">@color/couponTranslucentOverlay</item>
  </style>

</resources>
